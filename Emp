<div class="container-fluid py-4">
  <!-- Header Section -->
  <div class="row mb-4">
    <div class="col-12">
      <div class="card">
        <div class="card-header bg-primary text-white">
          <h4 class="mb-0">
            <i class="fas fa-users me-2"></i>Employee Management Dashboard
          </h4>
        </div>
        <div class="card-body">
          <!-- Action Buttons -->
          <div class="row mb-3">
            <div class="col-md-6">
              <button class="btn btn-success me-2" (click)="addNewEmployee()">
                <i class="fas fa-plus"></i> Add Employee
              </button>
              <button class="btn btn-info me-2" (click)="exportToCSV()">
                <i class="fas fa-download"></i> Export CSV
              </button>
              <button class="btn btn-warning" (click)="loadEmployees()">
                <i class="fas fa-refresh"></i> Refresh
              </button>
            </div>
            <div class="col-md-6">
              <div class="input-group">
                <span class="input-group-text">
                  <i class="fas fa-search"></i>
                </span>
                <input 
                  type="text" 
                  class="form-control" 
                  placeholder="Search employees..."
                  (input)="onSearchChanged($event)"
                >
              </div>
            </div>
          </div>

          <!-- ag-Grid -->
          <div class="row">
            <div class="col-12">
              <ag-grid-angular
                #agGrid
                style="width: 100%; height: 600px;"
                class="ag-theme-alpine"
                [columnDefs]="columnDefs"
                [rowData]="rowData"
                [defaultColDef]="defaultColDef"
                [pagination]="true"
                [paginationPageSize]="20"
                [rowSelection]="'single'"
                [animateRows]="true"
                [enableRangeSelection]="true"
                [suppressRowClickSelection]="true"
                (gridReady)="onGridReady($event)"
                (rowSelected)="onRowSelected($event)">
              </ag-grid-angular>
            </div>
          </div>

          <!-- Loading Indicator -->
          <div *ngIf="isLoading" class="text-center mt-3">
            <div class="spinner-border text-primary" role="status">
              <span class="visually-hidden">Loading...</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Selected Employee Info -->
  <div class="row" *ngIf="selectedEmployee">
    <div class="col-12">
      <div class="card">
        <div class="card-header">
          <h5>Selected Employee Details</h5>
        </div>
        <div class="card-body">
          <div class="row">
            <div class="col-md-2">
              <img [src]="selectedEmployee.image" class="img-fluid rounded-circle" alt="Employee">
            </div>
            <div class="col-md-10">
              <h6>{{selectedEmployee.firstName}} {{selectedEmployee.lastName}}</h6>
              <p><strong>Email:</strong> {{selectedEmployee.email}}</p>
              <p><strong>Position:</strong> {{selectedEmployee.company.title}}</p>
              <p><strong>Department:</strong> {{selectedEmployee.company.department}}</p>
              <p><strong>University:</strong> {{selectedEmployee.university}}</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
